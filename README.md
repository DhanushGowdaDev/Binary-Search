# Binary Search in C 🔍

This is a simple **Binary Search program written in C**.
It searches an element in a **sorted array** using the Binary Search technique.

## ✅ Features
- Takes input of `n` elements (sorted)
- Searches for a key element
- Displays the position if found
- Shows total number of iterations

## 🧠 What is Binary Search?
Binary Search is an efficient searching algorithm that works on a **sorted array**.
